{
  "description": "simple live editor with monacode",
  "license": "MIT",
  "main": "./index.min.js",
  "module": "./index.min.js",
  "name": "etcha",
  "scripts": {
    "build": "esbuild --bundle index.js --outfile=index.min.js --format=esm --minify",
    "start": "servor --browse --reload --editor",
    "prepublish": "npm run build"
  },
  "version": "0.0.1",
  "dependencies": {
    "monacode": "0.3.1"
  },
  "devDependencies": {
    "esbuild": "0.6.12",
    "servor": "4.0.2"
  }
}
